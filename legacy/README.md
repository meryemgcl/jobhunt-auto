# Legacy Modules

This directory contains experimental or retired code paths that are not part of
the production deterministic engine executed by `main.py`.

- `crewai/`: the previous CrewAI agent/task experiment and its prompt files.
- `prototypes/`: early mock scraper, matching-engine, and Celery/Redis compose experiments.

Install `requirements-legacy.txt` before running anything in this directory.
